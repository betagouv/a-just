name: GitHub Actions
on:
  push:
    branches: [ dev ]
  pull_request:
    branches: [ dev ]

jobs:
  start-tests:
    runs-on: ubuntu-latest
    steps:
      - run: echo "TEST PROJECT"
      - run: npm run test